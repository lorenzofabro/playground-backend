name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  build:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Server SSH connection. Running commands.
      uses: appleboy/ssh-action@master
      with:
        HOST: ${{ secrets.HOST }}
        USERNAME: ${{ secrets.USERNAME }}
        PORT: ${{ secrets.PORT }}
        KEY: ${{ secrets.SSHKEY }} 
        script: |
          cd /var/www/playground
<<<<<<< HEAD
          git pull origin main
=======
>>>>>>> 1323792ea9c0b33a6cf6dcb95803d7cdda6275be
          npm install
          npm run build
          pm2 restart 2
